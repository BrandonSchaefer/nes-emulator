Emulator:
    CPU
        Modes
        Interrupts
        Cycles inc when page crossed for modes

    PPU
        Missing everything!

    APU
        Missing everything!

    Mappers
        Missing everything!

    iNES
        Tests
        Done (should be!)

UI:
    Rendering the ppu
    Getting events for the controller
    Actually do this part!
